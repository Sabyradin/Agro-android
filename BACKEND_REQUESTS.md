# BACKEND_REQUESTS.md — backend-ке өзгеріс сұраныстары

Үндемей обход жасалмайды, үндемей ұмытылмайды. Backend өзгертілмейді — сұраныстар осында жинақталады.

1. **«Менің пікірлерім» эндпоинті** (`GET /user/my-reviews` тәрізді): қазір клиент сатып алушы тапсырыстары + қаралған жарнамалар тізімінен `GET /reviews/{announcement_id}` арқылы қолмен жинайды (N+1 сұраныс). Арнайы эндпоинт күйінде ғана толық дұрыс дерек болады (iOS-та да осы шешім қолданылған).
2. **`POST /auth/biometric` тіркеу** — мобайл клиент шақырады, route.py-де жоқ. Биометриялық логин мүмкін емес болып тұр.
3. **`main_image_url` және медиа-URL-дердің толық емес (relative) келуі** — resolver клиентте ұсталады, бірақ backend абсолют URL қайтарса, клиент жұқтырады қатар толықтыру.
4. **Чат URL-деріндегі literal CR/LF** — клиент санитайзер ұстайды (Flutter-де де солай), backend жағынан түзету сұралады.
5. **`connect_error` оқиғасының disconnect арқылы келуі** — iOS/Flutter клиенттер де осыны айналып өтеді; протоколды қалыпты CONNECT_ERROR ретінде жіберуді сұраймыз.