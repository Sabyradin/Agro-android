# ISSUES.md — API ғажайыптары және ашық сұрақтар

Әр API мінез-құлығына күмән/спекке сай келмеушілік осында жазылады. Шешілгені — [x] + жауабы.

1. **[resolved — Фаза 11] google-services.json плейсхолдер схемасымен қосылды** — `app/src/dev/google-services.json` (com.agroland.app.dev) + `app/src/prod/google-services.json` (com.agroland.app). Нақты Firebase конфигурациясы (project_id, api_key, mobilesdk_app_id) команда бергенше осы файлдарға қойылады; код өзгерісі қажет емес. Плейсхолдер жағдайында FCM токені алынбайды → POST /device жіберілмейді, push мүлде үнсіз ыдырайды (Flutter setupFlutterNotifications try/catch үлгісі). `mobilesdk_app_id` форматы плагин талабына сай (1:N:android:hex).
2. **[open] `POST /auth/biometric` backend route.py-де тіркелмеген** (спек айтады) — биометриялық логин best-effort, 404 → биометриялық кіру түймесін жасыру.
3. **[open] Privacy/Terms нақты URL-дері** — сайт `/terms`, `/policy` жойған; команда бергенше конфигте `about/privacy/terms` markdown жолдары қолданылады (CommonMarkdownView, Фаза 18).
4. **[open] Dev тест-аккаунттар** (дилер 7001112203 + individual) — Фаза 3 эмуляторлық тексеріске керек.
5. **[open] «Менің пікірлерім»** эндпоинті жоқ — клиент жағында жиналатын болады (спек §10), BACKEND_REQUESTS.md-ге де жазылды.
6. **[resolved — Фаза 12] Қолдау чаты (Flutter DealerSupportSection / ProfileSupport)** — профил хабына «Қолдау» жолы қосылды (Headset иконкасы, дилер/жеке айырмашылығынсыз — Flutter-дің екеуі де бірдей чатқа ашылады): ChatRoomRoute(otherUserId = 31, isSystemChat = true). kSupportUserId = 31 Flutter constants.dart-пен сай.
7. **[open] `GET /verification/status` жауабының `documents[]` форматы** — әр құжаттың ішкі өрістері (id/url/status) спекте толық көрсетілмеген; қазір тек саны есептеледі, құжат тізімінің толық UI-ы backend жауабы нақтыланғанда толықтырылады.
8. **[open] `PATCH /user/profile` жауабының формасы** — спек «толық профиль қайтады» дейді, бірақ кейбір backend маршруттары `{success: true}` ғана қайтаруы мүмкін; ProfileRepository осы жағдайда profile null болса Failure.Parsing емес, profile-ды қайта GET жасайды (updateProfile* әдістерінің барлығы refresh() шақырады — қауіпсіз).
9. **[resolved — Фаза 12 (user id) / open (статистика жолы)] currentUserId қолжетімділігі** — chat модуліне `CurrentUserProvider` (feature:chat/data) қосылды: token кешінен немесе GET /user/profile-тан id алады, ChatListViewModel/ChatRoomViewModel оны «Сіз:» префиксі мен bubble alignment үшін қолданады. Деталь бетіндегі иелік статистикасының жолы (views/calls/favorites/messages) әлі қосылмаған — marketplace қолданушы статистика UI-ын жасағанда осы провайдер қайта пайдаланылады (BACKEND: статистика өрістері деталь жауабында келеді деп спек айтады).
10. **[resolved — Фаза 12] Деталь бетіндегі «Чат» түймесі** — байланыс секциясында «Чат» + «Қоңырау шалу» екі батырма қатар (author_id > 0 болса). Чат → ChatRoomRoute(otherUserId = author_id, username = сатушы аты, announcementId = жарнама id) — Flutter-дің chat_room query parameters үлгісі. Қоңырау ACTION_DIAL арқылы (WebRTC қоңырау — Фаза 13). Гость қолданушы чат батырмасын басса — авторизация бетіне бағытталады.
11. **[resolved — Фаза 7] `GET /announcements` country_id параметрі** — сүзгіге локация қосылды: FilterPage CatalogPickerDialog (country/region/district) арқылы `country_id`/`region_id`/`district_id` жіберіледі. Flutter лентасының ұсынылатын жолында міндетті `country_id=4` — қазір қолданушы сүзгісі ретінде жіберіліп жатыр (app-region countryId-ден автоматты міндетті жіберу Flutter-де deprecation хабаршысымен қорғалған); эмуляторлық тексерістен кейін ұсынылатын лентаға app-region енгізілуі мүмкін.
12. **[open] `POST /ai/ad-content` DEV-те жауап бермейді** — барлық `lang` кодтарында `{"success":false,"error":{"message":"An unexpected error occurred"}}` қайтарады (AI қызметі өшірулі сияқты). CreateAdPage AI-тобы кнопкасы Failure.Http оқиғасын snackbar-ға шығарады, қызмет қосылғанда автоматты жұмыс істейді.
13. **[open] `POST /demands` дене пішімі белгісіз** — спек JSON дейді, бірақ басқа жарнама жасау маршруттары multipart. MakeOfferViewModel dual-mode: алдымен JSON жібереді, Failure.Http 400–422 келсе ештеңе жасалмағаны анық (4xx) — multipart form ретінде қайта жібереді.
14. **[open] Өңдеу режиміндегі сурет сақтау өрісі** — `existing_images` (URL тізімі үтірмен) атауы болжамды; Flutter коды бұл машинада жоқ, backend OpenAPI-да нақты аты жоқ. Қате атау болса өңдеуде суреттер жоғалады — эмуляторлық тексерісте нақтыланады.
15. **[open] Bulk-upload файл part атауы** — `file` атауы болжамды (OpenAPI-да `/announcements/bulk-upload` schema көрсетілмеген). 400 келсе `file` → `excel_file`/`document` нұсқалары байқалады.
16. **[open] `delivery_zone_ids` қайталанатын part форматы** — backend тізікті (`delivery_zone_ids=1&delivery_zone_ids=2`) күтуі болжамды; JSON-массив (`[1,2]`) түрі де мүмкін. Бизнес-аккаунт тексерілінгенде нақтыланады.
17. **[open] Bulk шаблон `lang` параметрі** — `kk/ru/en/zh` тікелей жіберіледі (бөлек endpoint JSON ішінде `language` алғысымен); backend `kz/ch` мапингін қажет етсе — 400 келгенде түзетіледі (жәшік жоқ басқа endpoint-терден айырмашылығы мүмкін).
18. **[open] `GET /cities` DEV-те 404** — спектегі Country→Region→City тізбегінің соңғы буыны жоқ; curl арқылы тексерілді (`{"detail":"Not Found"}`). App-region орнату ағыны Country → Region-мен шектеледі, `app_region_city_id` := region id (Flutter-дің AppRegionModel.cityId үйлесімділігі үшін). `/cities` пайда болса District деңгейі қосылады.
19. **[open] MAPS_API_KEY бос плейсхолдер** — `gradle.properties` `MAPS_API_KEY` қасиеті команда бергенше бос; картаның Google плиткалары жүктелмейді. LocationSelectionPage ешқашан stub болмайды: карта орнына ақпарат карточкасы көрсетіледі, GPS + reverse geocode + каталог таңдауы толық жұмыс істейді. Кілт берілгенде GoogleMap автоматты қосылады (код өзгерісі қажет емес, manifestPlaceholders арқылы).
20. **[resolved — Фаза 7] `POST /user/location` дене пішіні** — Flutter `mobile-app/lib/location/domain/location_model.dart` (toMap) арқылы нақтыланды: `house/street/country/area/province/locality` (бос емесдері) + `country_id/region_id/district_id` + міндетті `latitude/longitude` (картадан таңдалмаса 0.0 — backend district_id-ды өздігінен турындатады, Location fix 3d78fa9). `PATCH /user/location/{id}` Flutter-де жоқ — өңдеу = POST жаңа + DELETE ескі (refreshStale үлгісі); `address_id` (Address кестесі) мен `user_location_id` (сілтеме жолы) айырмашылығы сақталды — DELETE/CreateAd user_location_id пайдаланады. Жауап оқу: LocationModel.fromJson пішіні (parseLocation). 3-тен асқан мекенжай ескісі өшіріледі (Flutter maxAddresses=3). Ескерту: біздің LocationSelectionPage картасы — Flutter-дің MapsPage-і өлі код (yandex_mapkit өшірілген), нақты Flutter ағыны — тізім + GPS; карта SPEC/MASTER_PLAN «Картамен таңдау» бабы бойынша қалды және кілт жоқ болса дәл Flutter мінез-құлқына ыдырайды (тізім + GPS + reverse geocode).
21. **[resolved — Фаза 9] Halyk ePay өрісі** — SPEC «invoice_url» дейді, нақты backend/Flutter жауабы `payment_url` (POST /payments/halyk/init → {payment_url, invoice_id, amount, currency, mock}); Flutter эталоны бойынша `payment_url` оқылады. `mock=true` DEV ортасында WebView-сіз тікелей нәтижше бетіне жүреді. BCC legacy 3D Secure (POST /payment/generate) — backend HTML форманы P_SIGN (HMAC-SHA1) өзі құрып қайтарады, клиент WebView-та көрсетеді (Фаза 10 әмиян толтыруға дайын).
22. **[resolved — Фаза 10] Әмиян маршруттары: /business/\* vs /dealer/\*** — SPEC «/dealer/* — /business/* атауларының legacy алиасы, target /business/* напрямую» дейді, ал Flutter эталоны `/dealer/balance`, `/dealer/balance/transactions`, `/dealer/withdraw` шақырады. DEV-те екеуі де бар екені тексерілді (curl: `/business/balance` 401, `/business/withdraw` 405 — маршрут бар; 404 емес). SPEC бойынша `/business/*` нұсқалары қолданылды. Шығару кезінде баланс сұраныс кезінде емес, admin мақұлдағанда ғана кемиді (backend ledger); клиент тек {amount, bank_details:{iban, bank_name, bik?, account?}} жібереді, минималды сома 5 000 ₸ клиент жағынан алдын-ала тексеріледі (backend шектеуімен сәйкес).
23. **[resolved — Фаза 11] Push payload routing эталоны** — Flutter push_notification_service._handlePayload дәйек тізбегі Android портта толық қайталанды (PushPayloadRouter): verification_approved/rejected → верификация; balance_credited/withdraw_approved/withdraw_rejected → әмиян; review_request+order_id → тапсырыс; order_id → тапсырыс; event=chat+sender_id → чат бөлмесі; announcement_id → жарнама; notification_id+notification_type → хабарламалар тізімі; fallback → тізім. Қайталау қорғаны — payload-тан сортталған кілттерден id + 500 мс cooldown (Flutter _generateIdFromPayload/_isDuplicateNotification). Бос FCM токен ЕШҚАШАН POST /device етілмейді (backend EMPTY_FIREBASE_TOKEN).
24. **[resolved — Фаза 12] Push межелері: ChatRoom + Notifications** — MainActivity екі межені де жалғады: ChatRoom → ChatRoomRoute(roomId, otherUserId = sender_id, username = sender_name, isSystemChat), Notifications → NotificationsByTypeRoute(type). Чат бейджі — Socket.IO ChatSocketService.totalUnread (тек байланыс орнағанда өседі; push badge толық саны Фаза 18-де FCM payload unread қосқанда нақтыланады).
25. **[deviation — Фаза 12] Видеоны сығу (Transformer)** — Flutter video_compress пакетінің баламасы (androidx.media3 Transformer) әзірше қосылған жоқ: видео түп нұсқада жіберіледі (backend шектеуі 10 МБ клиент жағынан алдын-ала тексеріледі). Transformer интеграциясы полиш фазасында (Фаза 18) қосылады.
26. **[open — эмуляторлық тексеріске] «Сіз:» префиксі мен bubble alignment senderId-ді CurrentUserProvider id-мен салыстырады** — backend кейбір хабарламаларда sender_id-ді «алушы» ретінде жіберуі мүмкін (Flutter-де де осы мінез бар). DEV эмуляторлық чат тексерісінде нақты ағында дұрыс жақта тұрғаны тексеріледі; қате болса ChatRoomViewModel merge кезінде id-ны түзету ережесі қосылады.
27. **[deferred — Фаза 18] In-app PDF/файл қарау құралы** — чат файлдары қазір жүйелік Intent (ACTION_VIEW + FileProvider/URI) арқылы ашылады; сыртқы қолданба болmasa «файлды ашу мүмкін болмады» деген адами қате шығады. Встроенный қарау құралы (PDF) полиш фазасына қалды.
28. **[deviation — Фаза 13] TURN буындары: AppConfig қабаты жоқ** — Flutter-дегі 4 буынды тізбек (REST → signaling ice_servers → AppConfig env-TURN → STUN-only) Android портында 3 буынды (REST → signaling → STUN-only): env-конфиг енгізілмеген, AppConfig TURN мәні Flutter-де де dev ортада бос. `GET /turn/credentials` backend-test-те 404 қайтаратыны curl арқылы бекітілген — TurnCredentialsRepository silent fallback (null), қоңырау STUN-only немесе signaling ice_servers арқылы жұмыс істейді. Backend TURN қосса код өзгерісін қажет етпейді.
29. **[open — эмуляторлық тексеріске] Telecom + CallStyle + WebRTC қоңырау** — self-managed ConnectionService, addNewIncomingCall, CallStyle notification (API 31+), full-screen intent — барлығы try/catch деградациясымен жазылды (Telecom қолжетімсіз болса notification өзі жеткілікті). Екі эмулятор арасында нақты WebRTC қоңырау (дыбыс алмасу, ICE, reconnect) DEV backend call:* сигналингімен бірге тексерілуі керек; call:* server events-тері backend-test-те voice_call модулі қосылғанда ғана толық жұмыс істейді (қазір invite → 35с no-answer таймері «Жауап жоқ»-пен аяқталады — қате емес, деградация).
30. **[resolved — Фаза 13] Қоңырау кіру нүктелері** — чат бөлмесі AppBar телефон батырмасы (peer-чаттерде ғана, жүйелік чатта жоқ; қоңырау жүріп жатқанда disabled), деталь бетінің Қоңырау батырмасы ACTION_DIAL арқылы (телефон нөмірі бар жарнамаларда), профиль Қолдау чаты — жүйелік чат, қоңырау жоқ. Микрофон рұқсаты invite алдында сұралады (pending-invite үлгісі), accept жолында — экран бірінші көрінеді, рұқсат диалогы кейін (Flutter invite-flow паритеті).
31. **[deviation — Фаза 14] GET /banner (hero-slider) + POST /marketing/posts/{id}/view қолданылмайды** — Flutter эталонында бұл екі endpoint шын мәнінде шақырылмайды (hero-slider баннері кодтан шыққан, marketing view тек click-пен шектеледі): Android порты да сол эталонды ұстады, StoriesApi тек GET /banners/main, GET /stories, POST /stories/{id}/view, POST /marketing/posts/{id}/click қамтиды.
32. **[resolved — Фаза 15] Статик промо-карточкалардың мақсаттары** — «Жарнама құру» → CreateAd (guest → Auth); «Жарнама» (advertise) → MyAnnouncementsRoute("active") (өз жарнамасын таңдап, «Жарнама жылжыту» батырмасы арқылы AdvertiseAdRoute-қа — Flutter-де контекстсіз advertise маршруты модельді талап етіп crash жасайтыны үшін, ISSUES #33); «Көтерілгендер» (promoted) → PromotedAnnouncementsRoute (guest → Auth). «Қытай тауарлары» карточкасы Фаза 17-де ChinaCatalogRoute пайда болғанда жалғанады (HomeFeedPage параметрі дайын).
33. **[deviation — Фаза 15] Контекстсіз «Жарнама» CTA-сы және HotAnnouncementsPage қолжетімділігі** — (a) Flutter main_banner_carousel статик «advertise» карточкасы AppRoute.advertise-ді модельсіз push етеді, бірақ сол маршрут model query параметрін талап етеді (null-assertion crash) — Android порты бұны қауіпсіз ағынмен алмастырды: MyAnnouncements → OwnerAnnouncement «Жарнама жылжыту» → AdvertiseAdRoute(announcementId). (b) Flutter HotAnnouncementsPage маршруты еш жерде тіркелмеген (өлі код); Android спек паритеті үшін HotAnnouncementsRoute (type_ad=vip лентасы, AnnouncementsListPage қайта пайдаланылады) NavHost-та бар, бірақ Flutter-дегідей кіру нүктесі жоқ — кіру нүктесі кейінгі фазаларда UX шешімімен қосылады.
34. **[open — эмуляторлық тексеріске] Промо v2 каталог/белсендіру ағындары** — GET /promo-v2/catalog, per-SKU activate_endpoint (POST/PATCH/PUT диспетчер + {announcement_id} алмастыру), 409 BANNER_SLOT_ALREADY_ACTIVE → PATCH fallback (тек banner kind), POST /banners/main/image multipart `file` part атауы, GET /announcement/{id}/promotion пішімі, PATCH /promotion/{id}/auto-renewal денесі {auto_renewal} — барлығы Flutter эталонынан (promo_v2_repository.dart) 1:1 көшірілді, бірақ DEV backend-те нақты SKU-лардың жауаптарымен эмуляторлық тексеру талап етіледі (catalog {items:[]} бос болса «Тарифтар жоқ» көрінеді — қате емес).
35. **[improvement — Фаза 16] Тапсырыс трекингінің өлі маршруты тіркелді** — Flutter-де OrderTrackingPage маршруты бар, бірақ еш UI элементі оны ашпайды (өлі код). Android порты карточканың ашылатын бөліміне «Трекинг» TextButton қосты (OrderTrackingRoute(orderId) → GET /orders/{id}/tracking): дилер әр тапсырыстың логистика тарихын бір басумен көреді.
36. **[improvement — Фаза 16] Promotion табы Flutter TODO stub орнына нақты деректермен** — Flutter dealer_promotion_tab.dart «Белсенді жарнамалар» бөлігін TODO етіп қалдырған (тек пакет карточкалары статик түрде көрінеді). Android порты GET /user/announcements/promotions арқылы нақты белсенді промоларды көрсетеді (hasPromotion фильтрі, прогресс жолағы, қалған күндер), пакет карточкалары (Boost/VIP/Premium) dev flavor-да ғана — Flutter kIsProd паритеті.
37. **[deviation — Фаза 16] Аналитика тариф гейті in-tab** — Flutter-де аналитика кіру нүктесінде тариф диалогы шығады (showDialog), Android порты гейтті табтың өзіне орналастырды (AnalyticsLockedPlaceholder: Lock иконкасы + түсініктеме) — 4 таб хабы ішінде бөлек диалогның орнына бірден көрінеді. GET /subscription/current қатесі ешқашан UI-ды құлатпайды: FALLBACK шарттарымен жалғасады (analytics_enabled=false → locked placeholder).
38. **[open — эмуляторлық тексеріске] POST /dealer/employees қате денелері** — backend қате жағдайында JSON ({detail/error/message}) орнына жай string қайтуы мүмкін; DealerRepository екеуін де көтереді (адами мәтін шығады). ЖСН 12 цифр / телефон 11 цифр клиент валидациясымен қатар backend-тің нақты қате пішімі эмуляторлық тексеристе нақтыланады.
39. **[open — эмуляторлық тексеріске] Сұраныс маршруттары: /demands vs /demand/{id}** — SWIFT_REWRITE_SPEC L118 «GET/POST /demands, GET/PATCH/DELETE /demand/{id}, PATCH /demand/{id}/activate|/deactivate» дейді (тізім көпше, жеке жалғыз сияқты). DemandApi сол пішінде жазылды; эмуляторлық тексеристе 404/405 келсе түзетіледі. Дене JSON (ISSUES #13-тегі MakeOffer dual-mode жағдайын қайталамайды: сұраныста сурет/файл жоқ, сондықтан multipart вариантсыз).
40. **[open — эмуляторлық тексеріске] EGOV /agri-machinery/{vin} жазба пішімі** — қызмет каталогы статик (83 жазба, Flutter egov_service_model.dart 1:1), бірақ VIN сұрауының `data` өрістері белгісіз (дев-ортада әдетте SCSE002 қате конверті келеді). EgovParser толығымен generic: `data` объектінің primitive жолдары key-value ретінде көрсетіледі (қалған өрістер жасырылады), `status:"error"`/`data:null` → «Табылмады» + backend message. Нақты жазба пішіні белгіленгенде label локализациясы қосылуы мүмкін.
41. **[open — эмуляторлық тексеріске] Қытай каталогының DEV деректері** — MercuryX проксиі дев-ортада каталогты бос қайтаруы мүмкін (сүзгілер/тізімдер бос «—» емес, бос күй көрінеді). ChinaParser барлық жауап variantтарына толерантты (names JSON-строка, has_children `{cnt}`, string int, `{data:{data}}`/`{data}` ораулары — ChinaParserTest-пен қамтылған). Техникалық ескерту: Coil 3.2-де `ImageLoaderFactory` жоқ — Application `SingletonImageLoader.Factory` жүзеге асырады (coil bundle артефакты), SVG декодері глобальды singleton арқылы барлық CachedImage-ге қолданылады.
42. **[open — эмуляторлық тексеріске] Пікір эндпоинттерінің жауап пішіндері** — GET /reviews/{id}, GET /user/{id}/reviews, GET /seller/{id}/reviews, POST /review, like/unlike жауаптарының нақты конверттері DEV-те тексерілген жоқ. ReviewParser барлық `{data}`/`{items}`/bare-array варианттарын көтереді (ReviewParserTest-пен қамтылған); like жауабында `review_id` болмаса — repository оптимистік fallback қолданады (authoritative деп санамайды).
43. **[deviation — Фаза 18] «Менің пікірлерім» — клиенттік агрегация (iOS MyReviewsStore паритеті)** — Flutter-де FeedbackPopup (жеткізілген тапсырыстан кейін авто-сұраныс) негізгі ағын, бірақ спек §10 + iOS нұсқасы MyReviewsStore клиенттік агрегацияны ұстанады (iOS-ты «өте әдемі» шыққан эталон ретінде ұстау директивасы). Android iOS үлгісін ұстады: үміткерлер = buyer тапсырыстары (GET /orders?role=buyer, legacy тамыр + Smart Calculator items[] екеуі де) + локальды қаралған жарнамалар (ViewedAnnouncementsStore, DataStore); екі қойынды «Қалдырылмаған/Қалдырылған»; бейдж = pendingCount (чтобы тізімі жолы + профайл тақтасы). Flutter авто-сұраныс попапы Android портына енгізілген жоқ (BACKEND_REQUESTS.md-де «Менің пікірлерім» эндпоинті сұралған — серверлік агрегация келсе миграция оңай).
44. **[improvement — Фаза 18] In-app PDF қарау құралы (ISSUES #27 жабылды)** — чат PDF-тері енді сыртқы қолданбаға ұсынылмайды: PdfViewerPage (android.graphics.pdf.PdfRenderer) кешке жүктеп (OkHttp, кеш-файл shortcut) барлық бетті 1080px енімен Bitmap ретінде LazyColumn-да көрсетеді. API < 35-те PdfRenderer/Page AutoCloseable емес — қолмен try/finally close(). Үлкен PDF (100+ бет) жад шығыны: беттер толық bitmap тізімінде ұсталады; полиш фазасында бет-сербер қосылуы мүмкін (LruCache/пагинация).
45. **[parity — Фаза 18] Flutter profileReviews маршруты — өлі код** — Flutter ProfileReviewsPage маршруты тіркелген, бірақ еш navigator оны ашпайды (router grep). Android ProfileReviewsRoute-ты спек паритеті үшін қалдырды (бет толық жұмыс істейді, керек болса бір жол NavHost-тан ашылады).
46. **[note — Фаза 18] PhotoViewerRoute(List&lt;String&gt;) type-safe навигация** — kotlinx.serialization тұтас List&lt;String&gt;-ді route argument ретінде кодтайды (URL-encoded JSON query); 65K+ URI шегі үшін gallery URL-дері қысқа — іс жүзінде қауіпсіз, бірақ 20+ суретті галереяда тereзу шегі асқан жағдайда route-ты id-тізім арқылы өзгерту керек.
47. **[note — Фаза 18] QR парсер android.net.Uri-сіз** — Flutter Uri.tryParse parity үшін QrUrlParser таза string парсингке ауыстылды (path/query қолмен): plain JVM unit-тестте android.net.Uri «not mocked» болатыны үшін. Мінез-құлық Flutter-мен 1:1 (path /announcement/&lt;id&gt; → trailing digits regex → query announcementId fallback).
48. **[resolved — Фаза 19] POST /search/log (SearchQueryLogRequest) дене пішіні** — curl арқылы DEV-те нақтыланды: `{}` → 422 «body.query Field required»; `{"query":"test"}` → 200 `{"success":true}`; `category_id` қосымша қабылданады (белгісіз өрістер елемейді). Flutter бұл endpoint-ті ЕШҚАШАН шақырмайды (өлі спек бабы) — MASTER_PLAN Фаза 19 талабы бойынша Android порты қосты: HomeViewModel іздеу ұсыныстары сәтті келгенде fire-and-forget POST (гость іздері де логталады, auth optional). Monitoring trackSearch оқиғасынан бөлек (бұл — негізгі backend-ке жіберілетін нақты API шақыру).
49. **[note — Фаза 19] GET /stories/analytics/{active-hours|popular-searches|category-interest} — өткізілді** — бұл үш endpoint sales-manager панеліне тиесілі (SWIFT_REWRITE_MAPS: «sales-manager» router тобы), Flutter қолданбасында еш экран шақырмайды (router grep: тек спекте аталады). Мобильді қолданбада тұтынушы UI-ы жоқ — Android порты да қоспады (паритет: екеуі де жоқ). Sales-manager консолі әрекет етілсе BACKEND_REQUESTS арқылы қосылады.
50. **[note — Фаза 19] TikTok dev flavor өшірулі** — dev flavor-да TIKTOK_APP_ID/TT_APPID бос: TikTokAnalytics.init қасиеттер бос болса sync skip (Flutter dev конфиг паритеті, log «init skipped»); prod flavor Flutter main.json нақты мәндерімен толтырылған (appId=com.agroland.app, ttAppId=7677876072366063636). accessToken Android SDK клиент жағында қолданылмайды (TTConfig-те setter жоқ; iOS HMAC signing — iOS-only) — BuildConfig-те сақтау/толық паритет үшін ғана.
51. **[note — Фаза 19] Firebase google-services.json — плейсхолдер** — app/src/{dev,prod}/google-services.json плейсхолдер конфигтер (Фаза 11-ден, ISSUES #1): FirebaseAnalytics.getInstance плейсхолдерде инициализация сәтсіз болса FirebaseAnalyticsService try/catch → null (барлық шақырулар no-op, crash ешқашан). Команда нақты Firebase жобасының google-services.json-ін бергенде аналитика/Crashlytics код өзгеріссіз қосылады.
52. **[note — Фаза 19] trackAppClose шақырушысы жоқ — Flutter паритеті** — Flutter-де де trackAppClose() еш жерде шақырылмайды (өлі әдіс, болжамдыdetached lifecycle hook үшін дайындалған). Android MonitoringService әдісті ұсынады, бірақ шақырмайды — Application.onTerminate сенімді емес (жүйе процесс-ті өлтіреді). Екі платформа да app_close оқиғасын фоновоы жібермейді.
53. **[note — Фаза 19] 401 auto-logout trackLogout жібермейді** — Flutter AuthNotifier.signOut/deleteAccount trackLogout жібереді, бірақ 401 interceptor logout-ы жібермейді (тиікте тек күй өзгереді). Android дәл сол паритет: trackLogout тек SessionController.onLoggedOut (қолмен шығу) арқылы; AuthInterceptor.onUnauthorized (token refresh сәтсіз) тек Guest күйіне өтеді, мониторинг оқиғасыз.
54. **[resolved — Фаза 20] GET /app-version жауап пішіні — curl арқылы DEV-те нақтыланды** — `?platform=android&current_version=X` → 200 `{"platform":"android","version":"1.0.35","min_version":"1.0.36","force_update":true,"update_message":"","update_required":true,"is_active":true}`. Платформа бойынша ЖАЛҒЫЗ жазба (current_version жауапқа әсер етмейді — сервер әрқашан бірдей конверт қайтарады), сондықтан Flutter-дің толық клиенттік шешім-логикасы (_compareVersions + target/min_version қадамдары) Android-та `decideUpdate` таза функциясында 1:1 қайталанды (AppVersionLogicTest — 13 кейс). DEV қазір force-update тестін көрсетеді: target=1.0.35, min=1.0.36 → Flutter iOS/Android 1.0.35-те тұрғандар міндетті диалог алады; Android портының dev-нұсқасы (1.0.0-dev) — ескі нұсқа ретінде force_update=true диалогын көрсетеді — ДҰРЫС мінез (Flutter-дің dev build-і де солай). update_message серверде бос — диалог қосымша тіліндегі updateMessage мәтінін көрсетеді (Flutter шешімі 1:1).
55. **[note — Фаза 20] Рұқсаттар аудиті (Flutter AndroidManifest паритеті)** — app: INTERNET, ACCESS_COARSE/FINE_LOCATION (өңір таңдау/карта/reverse geocode), POST_NOTIFICATIONS (push), CAMERA (QR сканер), USE_FULL_SCREEN_INTENT (қоңырау/пуш, API 33+ рұқсат сұрауы MainActivity-де); call: RECORD_AUDIO, MODIFY_AUDIO_SETTINGS, READ_PHONE_STATE (Telecom ConnectionService state), MANAGE_OWN_CALLS, FOREGROUND_SERVICE + FOREGROUND_SERVICE_PHONE_CALL (self-managed calls); chat: RECORD_AUDIO + ACCESS_FINE_LOCATION (дауыстық хабарлама, current location share). Барлығы Flutter android/app/src/main/AndroidManifest.xml + feature манифесттерімен 1:1, Play Console-да дәлелдеу мәтіндерін қажет етеді (бірнеше көрсеткіш: камера, микрофон, дәл локация, фондық қоңырау).
56. **[note — Фаза 20] Play Console дайындық** — bundleProdRelease AAB шығады (R8 minify+shrink, proguard-rules.pro TikTok/Firebase/serialization keep-ерімен); signing қазір debug keystore fallback — команда Play App Signing upload кілтін (keystore/пароль) бергенше gradle.properties арқылы `SIGNING_*` қасиеттерін қосу қажет (код дайын, build.gradle.kts signingConfigs блогы қосылмаған — арнайы кілт берілгенде ғана қосылады). google-services.json плейсхолдер (ISSUES #1/#51) — Playке шығарар алдында нақты Firebase конфиг міндетті (push + аналитика үшін). versionCode/versionName (1/1.0.0) — команданың release каденциясымен жаңартылады.
57. **[note — Фаза 20] edge-to-edge** — MainActivity.enableEdgeToEdge() (Activity 1.10): жүйелік баптар мөлдір, құрама M3 Scaffold/TopAppBar өздері insets қолданады (AgroScaffold = Material3 Scaffold, auto insets); қалған overlay-лер (VoiceCallScreen, StoryViewer, PIN қақпасы) Box үстіңгі қабаттары — өздерінің соқпаларын қолданады. Splash экран (installSplashScreen) API 31+ edge-to-edge-пен үйлеседі.
58. **[resolved — UI полиш] RegionListPage өңірдің орнына ЕЛДЕР тізімін ашатын** — `CatalogPageViewModel` маршрут аргументін `savedStateHandle.get<Int>("country_id")` деп snake_case кілтпен оқыған, ал type-safe навигацияда кілт — Kotlin қасиетінің аты (`countryId`). Мән әрқашан null болып, ViewModel `getRegions` орнына `getCountries` шақыратын: алғашқы іске қосуда «Қазақстан» таңдағанда қайтадан Қазақстан/Өзбекстан көрінетін. Түзетілді (екі жазылу да оқылады); эмуляторда тексерілді — енді нақты 17 облыс/қала келеді. Басқа ViewModel-дерде (chat/china/dealer/demand/reviews/marketplace) кілттер camelCase — олар дұрыс.
59. **[resolved — UI полиш] AgroSearchField мәтінді тігінен қиятын** — өріс Material `OutlinedTextField`-ке мәжбүрлі `.height(46.dp)` берген, ал оның ең кіші биіктігі 56dp: hint пен енгізілген мәтіннің асты кесіліп, «Іздеу» → «Ізлеv» болып көрінетін (басты бет, сервистер, чат — барлық іздеу өрісі). `BasicTextField` + өз контейнеріне (48dp, pill, `grey` толтыру) ауыстырылды. Толтыру `card` ЕМЕС `grey`: өріс көбіне ақ/қою сұр картаның үстінде тұрады да, бірдей түспен фонға сіңіп жоғалатын (қараңғы темада мүлдем көрінбейтін).
60. **[resolved — UI полиш] SplashPage индикаторы 16dp нүктеге айналған** — `Modifier.size(40.dp).padding(top = 24.dp)` деп жазылған: соқпа 40dp-тің ІШІНЕН алынып, индикатор кішірейетін. Реті түзетілді (`padding` → `size`).
61. **[resolved — UI полиш] Edge-to-edge астында экрандар статус-барға кіріп кететін** — `enableEdgeToEdge()` Фаза 20-да қосылған (ISSUES #57), бірақ M3 Scaffold/TopAppBar қолданбайтын беттер (MainShell қойындылары, HomeFeed, Services, ChatList, Cart, Language, Auth/OTP/Register) өз соқпаларын қоспаған — іздеу жолағы мен тақырыптар сағаттың астында тұратын. Барлығына `statusBarsPadding()`/`navigationBarsPadding()`/`imePadding()` қосылды. Сонымен бірге жүйелік жолақ иконкаларының түсі енді қосымшаның ӨЗ темасына байланады (`enableEdgeToEdge(SystemBarStyle.auto { darkTheme })` + `values-night/themes.xml`): бұрын жүйе ашық, қосымша қараңғы режимде тұрғанда сағат/батарея көрінбейтін.
62. **[improvement — UI полиш] Басты бет пен төменгі навигация iOS макетіне келтірілді** — команда берген iOS скриншоттары эталон ретінде алынды: (a) төменгі навигация — жиекке бекітілген 5 слот (Басты бет · Чат · Қосу · Себет · Қызметтер), ортасында жасыл дөңгелек «+» өз атауымен (бұрын: жиектен бөлек қалқымалы пилл, «+» жолақтан жоғары шығып контентті жабатын); (b) жоғарғы аймақ — оң жақта QR/қоңырау/аватар, астында ірі «Agroland» тақырыбы және толық енді іздеу өрісі (бұрын: бір жолда аватар + іздеу + 4 иконка, іздеу өрісі сығылып қалатын); (c) үш сегментті iOS-ауыстырғышы (сұр контейнер, таңдалғаны ақ «таблетка» көлеңкесімен, таңдалмағандар арасында тік сызық) + оң жағында дөңгелек фильтр батырмасы; (d) лента — тік карточкалы 2 бағанды тор (шаршы сурет, үстінде VIP/Ыстық белгісі мен жүрек, астында қала · атау · баға · бөлгіш · күн) — бұрын көлденең жолақ карточкалар болатын; (e) баннерлер/сторилер/жылдам өтулер лентамен БІРГЕ скроллданады (бұрын қатып тұрып, лентаға экранның азғана бөлігі қалатын). Таб атаулары да iOS-пен теңестірілді: «Agro Market», «MercuryX» (бұрын «Agro маркет», «Қытай тауарлары» — соңғысы пиллге сыймай ортасынан қиылатын).
63. **[improvement — UI полиш] Ортақ компоненттер мен дизайн токендері** — `AgroSpacing/AgroRadius/AgroSize` (core:ui/theme/AgroDimens.kt) енгізілді; `AgroCard`, `AgroSectionHeader`, `AgroDivider`, `AgroIconCircle`, `GuestGate`, `ShimmerGridCard`, `AgroOutlinedButton` қосылды. Бос/қате/қонақ күйлері бір қаңқаға (`StatePlaceholder`) көшті: жұмсақ дөңгелектегі иконка + қалың тақырып + ені шектелген батырма (бұрын «Кіру» экранның бүкіл енін алатын жасыл жолақ болатын). `CachedImage` енді плейсхолдер салады — сурет жүктелмегенде карточка бос/«сынған» болып көрінбейді (офлайн режимде баннерлер тұтас жасыл шаршыға айналатын).
64. **[improvement — UI полиш] Нақты Agroland логотипі** — бұрын splash/launcher/auth беттерінде уақытша «A» әрпі мен «Agroland» жазуы тұрған (ISSUES #1). Flutter `assets/icons/app-logo-big.svg` пен `logo.svg` векторлық drawable-ға түрлендірілді (сәндік blur маскалары алынып тасталды, қалғаны 1:1): `core:ui/drawable/ic_agroland_logo.xml` (толық лого — auth/тіл беттері), `ic_agroland_mark.xml` (белгі), `app/drawable/ic_splash_logo.xml` (жүйелік splash — жасыл фондағы ақ белгі), `ic_launcher_foreground.xml` (адаптивті иконка). Legacy mipmap PNG-лері (API 25) әлі плейсхолдер — растр генерациясы қажет.
65. **[note — UI полиш] Эмуляторда интернет жоқ — лента нақты backend-пен тексерілген жоқ** — бұл машинадағы AVD-де әдепкі маршрут (default route) орнатылмайды (`ip route`-та тек link-local жазбалар; cold boot та, wifi toggle да көмектеспеді; adb root жабық — қолмен route қосу мүмкін емес). Хост интернетте, эмулятор 10.0.2.2-ге жетеді, бірақ сыртқы желіге шықпайды — бұл AVD/хост NAT мәселесі, қосымшаға қатысы жоқ. Лента карточкалары мен 2 бағанды тор уақытша локаль мок-API (10.0.2.2) арқылы нақты деректермен көзбен тексерілді; DEV backend-пен толық тексеру желісі бар машинада қайталануы керек (әсіресе: VIP/Ыстық белгілері, ұзын атаулар, «Жеткізу» жолы, пагинация).
66. **[resolved — БЛОКЕР] Кіру мүлдем жүрмейтін: телефон нөмірі қате пішінде жіберілген** — Flutter/iOS `getCode` нөмірді `'+' + phoneCode + digits` түрінде жинайды (мыс. `+77011234567`), ал Android порты пайдаланушы терген жолды сол күйі `{"phone_number": ...}` ішіне салатын (`7011234567` немесе `87011234567`). Backend мұндай нөмірмен аккаунтты таппай, үш тест аккаунттың бірде-бірі кіре алмаған. Түзетілді: `CountryPhoneMask` (core:common, Flutter `assets/config/countries.json` 1:1 — KZ/RU/UZ/CN/KG) + `AgroPhoneField` (core:ui) енгізілді; өріс ел кодын таңдатып, тек ұлттық цифрларды ұстайды, ал жіберер алдында `toE164()` толық пішінді құрайды. Кіру де, тіркелу де осы жолмен жүреді. Эмуляторда тексерілді: сұрау денесі енді `{"phone_number":"+77011234567"}`.
67. **[resolved — UI] Сторис жолы мен viewer бос/қара болып көрінетін** — DEV backend `GET /banners/main` → `{"items":[]}` және `GET /stories` → `{"stories":[]}` қайтарады (баннер де, стори де жоқ). Бұрын мұндай жағдайда код Wikimedia сток-суреттерін қоятын — олар құрылғыда жүктелмей, жол сұр плейсхолдерге, viewer қара экранға айналатын («стористер ашпай тұр»). Жалған fallback толығымен алынып тасталды: backend бос болса, жол 4 статик промо-карточканы көрсетеді (Құру · Көтеру · MercuryX · Топта) — олар әрқашан салынады әрі нақты әрекетке апарады. Атаулар 84dp карточкаға сыятын қысқа нұсқаға ауыстырылды (бір ұзын сөз екі жолға тасымалданбайды — «Көтерілгендер» бір жолда қиылатын).
68. **[improvement — iOS паритеті] Лента карточкасында «Себетке» батырмасы** — iOS Agro Market карточкасында толық енді жасыл «Себетке» батырмасы бар. Қосылды: `allow_cart = true` жарнамаларда ғана көрінеді (recommended лентасында бұл өріс false, дилер лентасында true — iOS-тағыдай). `feature:cart` → `feature:marketplace`-ке тәуелді болғандықтан кері тәуелділік мүмкін емес: батырма колбэк арқылы app модуліне шығарылып, жаңа `QuickCartViewModel` (id + өлшем бірлігі, саны 1 — Flutter `addToCart` паритеті) арқылы орындалады; нәтижесі тост түрінде көрінеді, қонақ режимінде Auth бетіне бағыттайды.
69. **[resolved — UI] Кіру беті iOS макетіне келтірілді** — жоғарыда «Жабу», ортада ақ карточкадағы нақты AGROLAND логотипі, «Кіру» тақырыбы + түсіндірмесі, ту мен ел коды бар маскаланған телефон өрісі, астында «Әрі қарай» + «Тіркелу». Логотип ассеті: Flutter `assets/images/app_icon.png` ақ жиектерінен кесіліп, 512px webp ретінде `core:ui`-ге қосылды (33 КБ); сол сурет лончер иконкасы мен жүйелік splash-қа да қойылды (фон — ақ). Бұрын мұнда `app-logo-big.svg`-тен алынған «AGRO FERMA» жазуы тұрған — ол Flutter-дің ескірген ассеті екені iOS скриншотынан анықталды.
70. **[resolved — ереже №2] Backend-тің ағылшын техникалық мәтіні UI-ға шығатын** — кіру сәтсіз болғанда экранда «User not found» деп ағылшынша көрінетін (MASTER_PLAN тығыздық-ережесі №2 бұзылған). `AuthViewModel.toAuthError` енді backend мәтінін тек локализацияланған (ASCII-ден тыс таңбасы бар) болса ғана өткізеді; USER_NOT_FOUND нақты жағдайында әрқашан `auth_user_not_found` көрсетіледі. Сонымен бірге қатенің астындағы екінші «Тіркелу» батырмасы алынды (беттің төменінде бәрібір бар еді) — ол қиылып тұратын.
71. **[note] Лента карточкасында қала атауы жоқ** — `GET /announcements` жауабындағы `location` тек ID-лерден тұрады (`{country_id, region_id, district_id, latitude, longitude}`), атау жолдары (`city`/`city_name`) мүлдем жоқ. Сондықтан карточкадағы орын жолы бос: иконка жалғыз ілініп тұрмауы үшін атау болмаса жол мүлдем салынбайды (биіктік сақталады — қатардағы карточкалар тегіс тұрады). iOS «Қазақстан» деп көрсетеді — демек ол ID-ді каталогтан атауға айналдырады; Android-та да солай істеу үшін `feature:location` каталогын лентаға жалғау керек (тәуелділік бар, бірақ кэш/резолвер жазылмаған).
72. **[note] Басты беттегі «Категориялар» жолы алынған** — бұл өткелде HomeFeedPage-тен «Категориялар / Барлығын көру» жолы (және `onOpenCategories` сымдары) алынып тасталды — iOS макетінде ондай жол жоқ. Категориялар фильтр бетінен қолжетімді (FilterPage категория/субкатегория таңдауын қамтиды), бірақ бөлек `CategoriesPage` енді еш жерден ашылмайды (өлі маршрут — ISSUES #35/#45 үлгісінде тіркеулі қалды).
73. **[resolved — iOS паритеті] Стористер жолында фото-стористер жоқ болатын** — backend `GET /banners/main` пен `GET /stories` бос, сондықтан жолда тек 4 жасыл статик карточка тұратын, ал iOS-та агро фото-стористер көрінеді. Flutter fallback URL-дері `thumb.wikimedia.org` хостына сілтейтін (сондықтан құрылғыда жүктелмейтін, ISSUES #67). 5 сурет (Wikimedia Commons, еркін лицензия) `feature/stories/res/drawable-nodpi`-ге кіріктірілді, `ViewerStory.imageRes` қосылды: backend баннері жоқ кезде карусель мен viewer осы суреттерді желісіз көрсетеді.
74. **[resolved — iOS паритеті] Жарнама беті + «Жазу / Хабарласу»** — AnnouncementDetailPage iOS құрылымына ауыстырылды (нүкте-индикаторлы галерея, негізгі ақпарат карточкасы: күні/баға/VIP·ҚҚС чиптері/орны/қаралым·сүйікті·ID, «Ақпарат | Сипаттама», «Жеткізу / Өзі алу», пікірлер, сатушы + «Пайдаланушының барлық жарнамалары», көлденең тізімдер). Себет тауарында — «Сатып алу / Себетке» (DetailBuyBar iOS стилінде); қалған жарнамада — «Жазу» (чат) + «Хабарласу» парағы (телефонға қоңырау + қолданба ішіндегі дауыстық қоңырау, микрофон рұқсатымен). MainActivity-де `rememberVoiceCallStarter` ортақ көмекшісі. Лента карточкаларында KZT → ₸, piece → дана.
75. **[resolved — БЛОКЕР] Чатта пернетақта енгізу өрісін жабатын** — edge-to-edge астында ChatRoomPage IME/навбар insets қолданбаған: жазып отырған мәтін көрінбейтін, өріс жүйелік навигация жолағының астында қалатын. Түбір Column-ға `WindowInsets.ime ∪ navigationBars` (Bottom) қосылды; тізім `reverseLayout` — ашылғанда соңғы хабарлама бірден көрінеді.
76. **[resolved] Әр хабарламада «null» жауап-цитатасы** — `ChatParser.strOrNull` JSON null-ды `JsonPrimitive.content == "null"` жолы ретінде қайтаратын. JsonNull және "null" енді null. Күн чипінде «Бүгін» орнына уақыт шығатын (buildRows `now` орнына күннің өзін беретін) — түзетілді.
77. **[resolved — БЛОКЕР] Дауыстық хабарлама мен камера жұмыс істемейтін** — RECORD_AUDIO рұқсаты ешқашан сұралмайтын (MediaRecorder үнсіз құлайтын); манифестте CAMERA жарияланғандықтан ACTION_IMAGE_CAPTURE/VIDEO_CAPTURE рұқсатсыз SecurityException лақтыратын. Екеуіне runtime рұқсат ағыны қосылды. Енгізу жолағы WhatsApp үлгісінде қайта жазылды: «+» → түсті тор (Галерея/Камера/Бейне/Құжат/Аудио/Орналасу), басып тұру → жазу, солға → болдырмау, жоғары → бекіту (тірі толқын-форм + жою/жіберу), қысқа түрту → кеңес. Орналасу кеште жоқ болса `getCurrentLocation`. Профиль жүктелмеген сәтте «Жіберу» үнсіз ештеңе істемейтін — енді жүктеп, қайта жібереді. Эмуляторда нақты DEV backend-пен тексерілді: мәтін, дауыс (4с), камера суреті жіберілді.
78. **[resolved] Чат көпіршектері мен свайп** — WhatsApp палитрасы (беж фон, ашық жасыл/ақ көпіршектер, көк ✓✓, қараңғы тақырып), уақыт мәтін ішінде, agroland.kz жарнама сілтемесінің превьюі, оңға сырғытып жауап беру, дауыс ойнатқышы (асинхронды prepare, сүйреп жылжыту, ұзақтық метадеректен). Чат тізіміндегі свайп: бұрын `snapTo` асинхронды болғандықтан офсет «секіретін» және 3 кішкентай дөңгелек иконка тұратын — енді толық биіктіктегі түсті панельдер (Дыбыссыз/Жою/Мұрағат, иконка + жазу), синхронды саусақ позициясы, жылдамдыққа сай серпімді анимация, толық свайп → мұрағат + діріл.
